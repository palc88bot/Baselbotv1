{
  "entities": {
    "TradingSignal": {
      "title": "Trading Signal",
      "description": "A signal generated by the quantum algorithm.",
      "type": "object",
      "properties": {
        "symbol": { "type": "string", "description": "The asset symbol (e.g., BTC/USDT)." },
        "type": { "type": "string", "enum": ["BUY", "SELL"], "description": "The type of signal." },
        "strength": { "type": "number", "description": "The strength of the signal (0-1)." },
        "reason": { "type": "string", "description": "The reason for the signal." },
        "timestamp": { "type": "number", "description": "Unix timestamp of when the signal was generated." }
      },
      "required": ["symbol", "type", "strength", "timestamp"]
    },
    "BotState": {
      "title": "Bot State",
      "description": "The current operational state of the trading bot.",
      "type": "object",
      "properties": {
        "isRunning": { "type": "boolean", "description": "Whether the autonomous trading is active." },
        "lastHeartbeat": { "type": "number", "description": "Unix timestamp of the last heartbeat." },
        "activeSymbols": { "type": "array", "items": { "type": "string" }, "description": "Symbols currently being monitored." }
      },
      "required": ["isRunning", "lastHeartbeat"]
    }
  },
  "firestore": {
    "/users/{userId}/signals/{signalId}": {
      "schema": "TradingSignal",
      "description": "User-specific real-time feed of trading signals."
    },
    "/users/{userId}/state/current": {
      "schema": "BotState",
      "description": "The current operational state of the user's trading bot."
    }
  }
}
